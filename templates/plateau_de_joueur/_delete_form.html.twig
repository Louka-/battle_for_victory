<form method="post" action="{{ path('plateau_de_joueur_delete', {'id': plateau_de_joueur.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer cette race?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plateau_de_joueur.id) }}">
    <button class="btn btn-danger rounded-pill">Supprimer</button>
</form>
