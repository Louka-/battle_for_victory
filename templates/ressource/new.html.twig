{% extends 'base.html.twig' %}

{% block title %}Ressource{% endblock %}

{% block body %}
	<div class="container mt-4">
		<div class="row justify-content-center">
        <img src="{{asset('images/fond-texte.png')}}" alt="" class="position-absolute">
			<div class="col-12">
				<h1 class="text-center my-5">Créer une nouvelle ressource</h1>

    {{ include('ressource/_form.html.twig') }}

        </div>
        <div class="d-flex justify-content-center pt-3">
            <a href="{{ path('ressource_index') }}" class="btn btn-info rounded-pill">Retour</a>
        </div>
    </div>
</div>
{% endblock %}
