{% extends 'base.html.twig' %}

{% block title %}Editer une Carte{% endblock %}

{% block body %}
	<div class="container mt-4">
		<div class="row justify-content-center">
		<img src="{{asset('images/fond-texte.png')}}" alt="" class="position-absolute">
			<div class="col-12">
				<h1 class="text-center my-5">Editer une carte</h1>

    			{{ include('carte/_form.html.twig', {'button_label': 'Enregistrer'}) }}


		</div>
		<div class="d-flex justify-content-center pt-3">
			{{ include('carte/_delete_form.html.twig') }}
        </div>
		<div class="d-flex justify-content-center pt-3">
            <a href="{{ path('carte_index') }}" class="btn btn-info rounded-pill">Retour</a>
        </div>
    </div>
</div>
{% endblock %}